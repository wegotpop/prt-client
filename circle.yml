dependencies:
  cache_directories:
    - ~/.cache/yarn
  override:
    - yarn
machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
test:
  override:
      - yarn test
  post:
      - yarn lint
